import React from "react";
const Cv = (props) => {
    return (
        <main>
            <section className="headerSection" style={{backgroundColor: "orange"}}>
                <h1>{props.title}</h1>
            </section>
            <section className="mainContentSection">
                
            </section>
            
        </main>
    );
};
export default Cv;