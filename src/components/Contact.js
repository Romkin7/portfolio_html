import React from "react";
const Contact = (props) => {
    return (
        <main>
           <section className="headerSection" style={{backgroundColor: "indigo"}}>
                <h1>{props.title}</h1>
            </section>
            <section className="mainContentSection">
                
            </section>
        </main>
    );
};
export default Contact;